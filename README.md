# JCA-Net
We plan to make our  all code publicly available once the paper is accepted.
# Installation
pip install torch torchvision
pip install -r setup/requirements.txt
# Requirements
Python >= 3.11<br>
PyTorch >= 2.1.2<br>
CUDA >=12.2<br>
Other dependencies in requirements.txt
# Experimental setup
Please check the config folder
